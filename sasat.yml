migration:
  dir: test/migrations
  table: __migration__
  out: test/out

generator:
  gql:
    subscription: true

db:
  host: $DB_HOST
  port: $DB_PORT
  user: $DB_USER
  password: $DB_PASSWORD
  database: $DATABASE
  connectionLimit: 10
